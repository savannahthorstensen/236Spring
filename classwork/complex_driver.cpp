#include "Complex.h";
#include <iostream>
using namespace std;

/*int main(){
    Complex c1;
    c1.print();
    c1.assign(2.3, 4.4);
    c1.print();
    c1.setReal(5.3);
    c1.print();
    cout << "c1.img: " << c1.getImg() << endl;
    c1.~Complex; //Destrctor can be called like other funtion 
                //but we dont need to do it bc it will be called automaticly when objects is destroyed
 






    return 0;
}
    */
   